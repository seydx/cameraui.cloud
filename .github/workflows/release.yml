name: Release

on:
  push:
    tags:
      - "v*"

jobs:
  build-binaries:
    name: Build binaries
    runs-on: ubuntu-latest
    env: { CGO_ENABLED: 0 }
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Go
        uses: actions/setup-go@v7
        with: { go-version: "stable" }

      - name: Resolve version from tag
        run: echo "VERSION=${GITHUB_REF_NAME#v}" >> "$GITHUB_ENV"

      - name: Build cloud_win64
        env: { GOOS: windows, GOARCH: amd64 }
        run: go build -ldflags "-s -w -X github.com/cameraui/tunnel/internal/app.Version=$VERSION" -trimpath -o tunnel.exe
      - name: Upload cloud_win64
        uses: actions/upload-artifact@v7
        with: { name: cloud_win64, path: tunnel.exe }

      - name: Build cloud_win32
        env: { GOOS: windows, GOARCH: 386 }
        run: go build -ldflags "-s -w -X github.com/cameraui/tunnel/internal/app.Version=$VERSION" -trimpath -o tunnel.exe
      - name: Upload cloud_win32
        uses: actions/upload-artifact@v7
        with: { name: cloud_win32, path: tunnel.exe }

      - name: Build cloud_win_arm64
        env: { GOOS: windows, GOARCH: arm64 }
        run: go build -ldflags "-s -w -X github.com/cameraui/tunnel/internal/app.Version=$VERSION" -trimpath -o tunnel.exe
      - name: Upload cloud_win_arm64
        uses: actions/upload-artifact@v7
        with: { name: cloud_win_arm64, path: tunnel.exe }

      - name: Build cloud_linux_amd64
        env: { GOOS: linux, GOARCH: amd64 }
        run: go build -ldflags "-s -w -X github.com/cameraui/tunnel/internal/app.Version=$VERSION" -trimpath -o tunnel
      - name: Upload cloud_linux_amd64
        uses: actions/upload-artifact@v7
        with: { name: cloud_linux_amd64, path: tunnel }

      - name: Build cloud_linux_i386
        env: { GOOS: linux, GOARCH: 386 }
        run: go build -ldflags "-s -w -X github.com/cameraui/tunnel/internal/app.Version=$VERSION" -trimpath -o tunnel
      - name: Upload cloud_linux_i386
        uses: actions/upload-artifact@v7
        with: { name: cloud_linux_i386, path: tunnel }

      - name: Build cloud_linux_arm64
        env: { GOOS: linux, GOARCH: arm64 }
        run: go build -ldflags "-s -w -X github.com/cameraui/tunnel/internal/app.Version=$VERSION" -trimpath -o tunnel
      - name: Upload cloud_linux_arm64
        uses: actions/upload-artifact@v7
        with: { name: cloud_linux_arm64, path: tunnel }

      - name: Build cloud_linux_arm
        env: { GOOS: linux, GOARCH: arm, GOARM: 7 }
        run: go build -ldflags "-s -w -X github.com/cameraui/tunnel/internal/app.Version=$VERSION" -trimpath -o tunnel
      - name: Upload cloud_linux_arm
        uses: actions/upload-artifact@v7
        with: { name: cloud_linux_arm, path: tunnel }

      - name: Build cloud_linux_armv6
        env: { GOOS: linux, GOARCH: arm, GOARM: 6 }
        run: go build -ldflags "-s -w -X github.com/cameraui/tunnel/internal/app.Version=$VERSION" -trimpath -o tunnel
      - name: Upload cloud_linux_armv6
        uses: actions/upload-artifact@v7
        with: { name: cloud_linux_armv6, path: tunnel }

      - name: Build cloud_linux_mipsel
        env: { GOOS: linux, GOARCH: mipsle }
        run: go build -ldflags "-s -w -X github.com/cameraui/tunnel/internal/app.Version=$VERSION" -trimpath -o tunnel
      - name: Upload cloud_linux_mipsel
        uses: actions/upload-artifact@v7
        with: { name: cloud_linux_mipsel, path: tunnel }

      - name: Build cloud_mac_amd64
        env: { GOOS: darwin, GOARCH: amd64 }
        run: go build -ldflags "-s -w -X github.com/cameraui/tunnel/internal/app.Version=$VERSION" -trimpath -o tunnel
      - name: Upload cloud_mac_amd64
        uses: actions/upload-artifact@v7
        with: { name: cloud_mac_amd64, path: tunnel }

      - name: Build cloud_mac_arm64
        env: { GOOS: darwin, GOARCH: arm64 }
        run: go build -ldflags "-s -w -X github.com/cameraui/tunnel/internal/app.Version=$VERSION" -trimpath -o tunnel
      - name: Upload cloud_mac_arm64
        uses: actions/upload-artifact@v7
        with: { name: cloud_mac_arm64, path: tunnel }

      - name: Build cloud_freebsd_amd64
        env: { GOOS: freebsd, GOARCH: amd64 }
        run: go build -ldflags "-s -w -X github.com/cameraui/tunnel/internal/app.Version=$VERSION" -trimpath -o tunnel
      - name: Upload cloud_freebsd_amd64
        uses: actions/upload-artifact@v7
        with: { name: cloud_freebsd_amd64, path: tunnel }

      - name: Build cloud_freebsd_arm64
        env: { GOOS: freebsd, GOARCH: arm64 }
        run: go build -ldflags "-s -w -X github.com/cameraui/tunnel/internal/app.Version=$VERSION" -trimpath -o tunnel
      - name: Upload cloud_freebsd_arm64
        uses: actions/upload-artifact@v7
        with: { name: cloud_freebsd_arm64, path: tunnel }

  package-and-release:
    name: Package and Release
    needs: build-binaries
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Download all artifacts
        uses: actions/download-artifact@v8
        with:
          path: artifacts

      - name: Resolve version from tag
        run: echo "VERSION=${GITHUB_REF_NAME#v}" >> "$GITHUB_ENV"

      - name: Create ZIP files with specific naming scheme
        run: |
          mkdir -p release

          # Function to get OS and architecture names for the filename
          function get_os_arch() {
            local artifact=$1
            
            if [[ $artifact == *win64* ]]; then
              echo "windows-x64"
            elif [[ $artifact == *win32* ]]; then
              echo "windows-x86"
            elif [[ $artifact == *win_arm64* ]]; then
              echo "windows-arm64"
            elif [[ $artifact == *linux_amd64* ]]; then
              echo "linux-x64"
            elif [[ $artifact == *linux_i386* ]]; then
              echo "linux-x86"
            elif [[ $artifact == *linux_arm64* ]]; then
              echo "linux-arm64"
            elif [[ $artifact == *linux_arm* && $artifact != *armv6* ]]; then
              echo "linux-armv7"
            elif [[ $artifact == *linux_armv6* ]]; then
              echo "linux-armv6"
            elif [[ $artifact == *linux_mipsel* ]]; then
              echo "linux-mipsel"
            elif [[ $artifact == *mac_amd64* ]]; then
              echo "darwin-x64"
            elif [[ $artifact == *mac_arm64* ]]; then
              echo "darwin-arm64"
            elif [[ $artifact == *freebsd_amd64* ]]; then
              echo "freebsd-x64"
            elif [[ $artifact == *freebsd_arm64* ]]; then
              echo "freebsd-arm64"
            else
              echo "unknown"
            fi
          }

          # Create a zip for each artifact with the proper naming scheme
          for dir in artifacts/*; do
            artifact=$(basename "$dir")
            os_arch=$(get_os_arch "$artifact")
            
            if [[ $os_arch != "unknown" ]]; then
              echo "Creating ZIP for $artifact as $os_arch"
              cd "$dir"
              
              # Prepare the filename: tunnel-darwin-x64-1.9.9.zip
              zip_filename="tunnel-${os_arch}-${VERSION}.zip"
              
              # Create the zip with the correct internal filename (just "tunnel" or "tunnel.exe")
              if [[ $os_arch == windows* ]]; then
                # For Windows, we want to keep it as .exe
                zip -j "../../release/$zip_filename" tunnel.exe
              else
                zip -j "../../release/$zip_filename" tunnel
              fi
              
              cd ../..
            fi
          done

      - name: Create Release
        id: create_release
        uses: softprops/action-gh-release@v3
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          files: release/*.zip
          draft: false
          prerelease: false
          tag_name: ${{ github.ref_name }}
          name: Release ${{ github.ref_name }}
